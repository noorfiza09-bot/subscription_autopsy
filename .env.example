# Local Postgres example — swap for your Neon/Supabase/Railway connection string
DATABASE_URL="postgresql://user:password@localhost:5432/subscription_autopsy"

# NextAuth (add once you wire up real auth)
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Email reminders (Resend) — sign up free at resend.com to get an API key.
RESEND_API_KEY=""
EMAIL_FROM="Subscription Autopsy <onboarding@resend.dev>"
# Random secret that protects /api/cron/reminders from being called by
# anyone who finds the URL. Generate with: openssl rand -base64 24
CRON_SECRET=""
